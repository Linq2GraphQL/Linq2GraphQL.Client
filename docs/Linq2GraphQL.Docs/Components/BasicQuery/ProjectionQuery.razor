@inherits SampleBase


<SamplesViewer 
    QueryExecute="@(starWarsClient
        .Query
        .AllFilms(first: 11)
        .Include(e => e.Films.Select(f => f.Producers))
        .Select(e => e.Films))"
        
    Query="@(@"starWarsClient
        .Query
        .AllFilms(first: 11)
        .Include(e => e.Films.Select(f => f.Producers))
        .Select(e => e.Films)")">
    
    
    
    </SamplesViewer>



